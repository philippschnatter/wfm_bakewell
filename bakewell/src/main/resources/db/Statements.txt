CREATE TABLE CUSTOMER(
ID INT PRIMARY KEY,
FIRSTNAME VARCHAR(255),
LASTNAME VARCHAR(255),
ADDRESS VARCHAR(255),
TELNO VARCHAR(255),
MAILADDRESS VARCHAR(255),
COMPANY VARCHAR(255),
PASSWORD VARCHAR(255)
);

CREATE TABLE INGREDIENT(
ID INT PRIMARY KEY,
NAME VARCHAR(255),
AMOUNT DOUBLE,
FAT DOUBLE,
SUGAR DOUBLE,
CALORIES DOUBLE
);

CREATE SEQUENCE IF NOT EXISTS ID_CUSTOMER_SEQ START WITH 1; 
CREATE SEQUENCE IF NOT EXISTS ID_INGREDIENT_SEQ START WITH 1;

DROP TABLE CUSTOMER;
DROP TABLE INGREDIENT;
DROP SEQUENCE ID_CUSTOMER_SEQ;
DROP SEQUENCE ID_INGREDIENT_SEQ;

INSERT INTO CUSTOMER (ID, FIRSTNAME, LASTNAME , ADDRESS , TELNO , MAILADDRESS , COMPANY , PASSWORD ) VALUES (ID_CUSTOMER_SEQ.nextval , 'a', 'b','c','d','e','f','g');